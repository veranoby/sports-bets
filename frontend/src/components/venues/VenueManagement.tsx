import React from "react";

const VenueManagement: React.FC = () => {
  return (
    <div className="card-background p-6">
      <h2 className="text-2xl font-bold text-theme-primary">
        Gestión de Galleras (Placeholder)
      </h2>
      <p>Este es un componente placeholder para la gestión de galleras.</p>
      <p>
        La lógica completa se extraerá de `frontend/src/pages/admin/Venues.tsx`
        en una fase posterior de optimización.
      </p>
    </div>
  );
};

export default VenueManagement;
